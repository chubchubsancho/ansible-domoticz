---
- name: "import specific variables for {{ ansible_distribution }} systems"
  include_vars: "{{ ansible_os_family }}.yml"
  tags:
    - domoticz
    - installation

- name: "install domoticz on {{ ansible_distribution }} systems"
  include_tasks: "{{ ansible_os_family }}.yml"
  tags:
    - domoticz
    - installation